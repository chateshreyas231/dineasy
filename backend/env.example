# Server Configuration
PORT=3001
NODE_ENV=development

# Database
# Option 1: PostgreSQL (recommended for production)
DATABASE_URL="postgresql://user:password@localhost:5432/reservation_agent?schema=public"

# Option 2: SQLite (easier for development - uncomment to use)
# DATABASE_URL="file:./dev.db"

# Redis (optional - app works without it, just no caching)
REDIS_URL="redis://localhost:6379"
REDIS_TTL=600

# Email (Nodemailer) - Optional for MVP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
EMAIL_FROM=noreply@reservationagent.com

# Platform API Keys (optional for MVP - most adapters use sample data)
OPENTABLE_API_KEY=
RESY_API_KEY=
YELP_API_KEY=
GOOGLE_PLACES_API_KEY=

# CORS
FRONTEND_URL=http://localhost:3000


